{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Cylunex/shadow-app/docs/module.schema.json",
  "title": "Shadow App Platform Catalog projection",
  "type": "object",
  "required": ["schemaVersion", "platform", "modules"],
  "additionalProperties": false,
  "properties": {
    "schemaVersion": { "const": 4 },
    "platform": {
      "type": "object",
      "additionalProperties": false,
      "required": ["catalogVersion", "deploymentId", "buildId", "identityIssuer"],
      "properties": {
        "catalogVersion": { "const": 1 },
        "deploymentId": { "type": "string", "pattern": "^[a-z][a-z0-9-]{1,63}$" },
        "buildId": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "identityIssuer": { "$ref": "#/$defs/httpsUrl" }
      }
    },
    "modules": {
      "type": "array",
      "items": { "$ref": "#/$defs/module" }
    }
  },
  "$defs": {
    "url": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://"
    },
    "httpsUrl": {
      "type": "string",
      "format": "uri",
      "pattern": "^https://"
    },
    "module": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id", "product_id", "name", "description", "canonical_url", "aliases", "auth",
        "health_path", "icon", "color", "order", "enabled", "capabilities"
      ],
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]{1,31}$" },
        "product_id": { "type": "string", "pattern": "^shadow-[a-z][a-z0-9-]{1,56}$" },
        "plugin_id": {
          "anyOf": [
            { "type": "string", "pattern": "^shadow-[a-z][a-z0-9-]{1,56}$" },
            { "type": "null" }
          ]
        },
        "name": { "type": "string", "minLength": 1, "maxLength": 24 },
        "description": { "type": "string", "maxLength": 80 },
        "canonical_url": { "$ref": "#/$defs/httpsUrl" },
        "aliases": {
          "type": "array",
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/url" }
        },
        "auth": {
          "type": "object",
          "additionalProperties": false,
          "required": ["mode", "groups"],
          "properties": {
            "mode": {
              "enum": [
                "public", "public-with-protected-paths", "forward-auth", "oidc",
                "service-bearer"
              ]
            },
            "groups": {
              "type": "array",
              "uniqueItems": true,
              "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]{1,63}$" }
            }
          }
        },
        "health_path": {
          "anyOf": [
            { "type": "string", "pattern": "^/" },
            { "type": "null" }
          ]
        },
        "icon": {
          "enum": [
            "app", "archive", "garden", "health", "ledger",
            "platform", "stock", "travel", "verse", "wingman"
          ]
        },
        "color": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" },
        "order": { "type": "integer", "minimum": 0, "maximum": 10000 },
        "enabled": { "type": "boolean" },
        "capabilities": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*(?:\\.[a-z][a-z0-9-]*)*$" }
        }
      }
    }
  }
}
